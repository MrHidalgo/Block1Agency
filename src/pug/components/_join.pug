// JOIN :: START

section#join.join
  .c-grid
    .join__wrapper
      .join__wrapper-left
        h1.join__title.title.viewport-hide-js!= "Join our exclusive <strong>blockchain</strong> clientelle."

        p.join__text.viewport-hide-js(
        data-animation-delay="0.2s"
        )!= "Get all the benefits of an in-wall or in-ceiling speaker system without any of the hassle. Just replace a few lights around your house, and start streaming crisp, clear audio, everywhere."

        .join__btn-wrap.viewport-hide-js(
        data-animation-delay="0.3s"
        )
          a(
          href="#"
          title=""
          class="join__btn btn btn--bg"
          )!= "Why wait?"

      .join__wrapper-right

        .join__box
          .join__block.join__block-0.viewport-hide-js(
          data-animation-name="fadeIn"
          )
          .join__block.join__block-1.viewport-hide-js(
          data-animation-name="slideInUp"
          )
            div
              +iconSVG("helmet")

          .join__block.join__block-2.viewport-hide-js(
          data-animation-name="slideInRight"
          )
            p!= "Bulldog <br>Exchange <br>ICO"
            img(
            src="./img/img-buldog.png"
            srcset="./img/img-buldog@2x.png 2x"
            alt=""
            title=""
            )
          .join__block.join__block-3.viewport-hide-js(
          data-animation-name="slideInLeft"
          )
            .join__block-head
              div
                .join__block-line
                p!= "Let’s work together."
              div
                +iconSVG("icecream")


            .join__block-body
              p Some Services

            .join__block-footer
              a(
              href="#" title="" class=""
              )!= "Logo Design"
              a(
              href="#" title="" class=""
              )!= "Video and Animation"
              a(
              href="#" title="" class=""
              )!= "Whitepaper Design"
              a(
              href="#" title="" class=""
              )!= "Web/UI/UX"


// JOIN :: END